# LDPC fixture

`bg2-z72-input.bin` and `bg2-z72-codeword.bin` are an independently generated
base-graph-2, lifting-size-72 decoder vector. They were generated for this
project and are distributed under the repository's MIT license.

The original source commit contains no external provenance to attribute.
